/**
 * Who wrote this software, as opposed to who runs this installation.
 *
 * The footer credit used to read `site_name`, so every installation on earth
 * announced that it had been built by itself: "Built with <heart> by Acme
 * Games" on Acme's own site. The operator's name already has a home in the
 * copyright line beside it. This one names the vendor and is the same on
 * every install, which is why it is a constant and not a setting.
 */
export const VENDOR_NAME = "Inovixi";

/** Where the credit points. Trailing slash kept: it is the canonical form. */
export const VENDOR_URL = "https://inovixi.com/";
